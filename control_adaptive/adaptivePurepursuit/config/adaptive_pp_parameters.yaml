Controller:
  ros__parameters:
    time_step: 0.1

    speed:
      maximum: 3.5

      minimum: 1.0

    gains:
      proportional: 2.6

      integral: 0.0

      differential: 2.3
      #lookAhead: 0.22
      lookAhead: 1.7
      #lookAhead: 0.3

    constants:
      # speed: 17.73
      speed: 0.15
      #lookahead: 0.4
      lookahead: 1.8
      #lookahead: 10.0 ew3a tgrbha_ana hzrtk

    drive: "/ackr"

    state: "/zed/zed_node/odom" #will be changed through phases

    path: "/path"  #will be changed through phases
