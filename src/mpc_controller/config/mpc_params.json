{
  "description": "MPC Controller Configuration Parameters for FSAI 2026",
  "version": "1.0",
  
  "prediction": {
    "horizon": 50,
    "dt": 0.02,
    "description": "Prediction horizon: 50 steps at 0.02s = 1.0 second lookahead at 50 Hz"
  },
  
  "vehicle": {
    "wheelbase": 2.5,
    "description": "Distance from rear axle to front axle in meters"
  },
  
  "cost_weights": {
    "Q_diagonal": [1.0, 1.0, 10.0, 0.1],
    "Q_description": "State tracking cost [x, y, theta, delta]",
    
    "R_diagonal": [0.1, 0.5],
    "R_description": "Control effort cost [v, delta_dot]",
    
    "Q_terminal_multiplier": 2.0,
    "terminal_description": "Terminal cost = Q * multiplier"
  },
  
  "constraints": {
    "velocity": {
      "v_min": 0.0,
      "v_max": 5.0,
      "unit": "m/s",
      "description": "Velocity bounds (0 to 5 m/s = 0 to 18 km/h)"
    },
    
    "steering": {
      "delta_max": 0.5236,
      "delta_max_degrees": 30.0,
      "description": "Maximum steering angle (±30° = ±0.5236 rad)"
    },
    
    "steering_rate": {
      "delta_dot_max": 1.0472,
      "delta_dot_max_degrees_per_sec": 60.0,
      "description": "Maximum steering rate (60°/s = 1.0472 rad/s)"
    }
  },
  
  "track_constraints": {
    "default_inner_width": 1.5,
    "default_outer_width": 1.5,
    "unit": "meters",
    "description": "Default track width constraints from centerline"
  },
  
  "tuning_notes": {
    "horizon": "Increase for smoother predictions (80-100), decrease for faster computation (30-40)",
    "Q_theta": "Increase Q(2,2) for better line tracking, decrease for smoother control",
    "R_values": "Large R = smooth control, Small R = aggressive control",
    "v_max": "Adjust based on testing: 2-3 m/s (low speed), 4-6 m/s (medium), 8-12 m/s (high speed)"
  }
}
